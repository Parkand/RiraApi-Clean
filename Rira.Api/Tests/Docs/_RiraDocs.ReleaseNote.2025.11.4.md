# 📦 RiRaDocs Teaching Edition – Release Meta File  
**Tag:** `RiraDocs-v2025.11.4-Stable-Final-Fixed`  
**Edition:** Teaching / Persian Inline Edition  
**Author:** سروش  
**Release Date:** 2025‑11‑04  
---

## 🧭 ۱. خلاصهٔ انتشار (Release Summary)

این نسخه شامل مستندسازی نهایی آموزشی برای کل معماری پروژه **RiRa** است که با سبک RiRaDocs Teaching Edition تدوین شده.  
تمامی فایل‌ها دارای کامنت‌های فارسی توضیحی درون‌خطی هستند، بدون هیچ تغییر در کد اجرایی واقعی.  
هدف این مجموعه‌، تبدیل زیربنای معماری ریرا به یک مرجع آموزشی کامل برای توسعه‌دهندگان مبتدی و پیشرفته است.

---

## 🧩 ۲. فهرست لایه‌ها و اجزای مستندشده  

| لایه | فایل‌های کلیدی | توضیح عملکرد |
|------|----------------|---------------|
| **Domain Layer** | `TaskEntity.cs`, `TaskPriority.cs`, `TaskStatus.cs` | معرفی ساختار موجودیت‌ها، منطق حذف نرم، چرخهٔ حیات و Enumهای دامنه |
| **Persistence Layer** | `EmployeeConfiguration.cs`, `AppDbContext.cs`, `TaskRepository.cs` | پیکربندی EF Core 8،  Seed Data، و توضیح تزریق وابستگی (DI) |
| **Application Layer (Common & Services)** | `ResponseModel.cs`, `ResponseModelTests.cs` | استانداردسازی خروجی سرویس‌ها با ResponseModel و تست کامل رفتار |
| **Test Utilities** | `EFAsyncMockHelper.cs`, `TestAsyncEnumerator.cs`, `TestAsyncQueryProvider.cs`, `InMemoryContextFactory.cs` | ابزارهای شبیه‌سازی EF Core برای Unit Test‌ها (پشتیبانی از async و ID خودکار) |
| **Integration Tests** | `TaskServiceIntegrationTests.cs` | تست‌های کامل CRUD با DI Container، AutoMapper، Validator و MediatR |
| **Exceptions Tests** | `BusinessExceptionTests.cs`, `NotFoundExceptionTests.cs` | تست ساختار پیام‌های خطا به زبان فارسی و سازنده‌های ترکیبی |
| **Commons / Imports** | `GlobalUsings.cs` | مدیریت یکپارچهٔ فضای‌نام‌ها برای کل تست‌ها و پروژه |

---

## 🧠 ۳. نکات معماری و آموزشی کلیدی  

- مستندسازی بر پایهٔ **نظریه Clean Architecture** و تجربهٔ پیاده‌سازی در ریرا انجام شده است.  
- تمام تست‌های واحد و ادغام با **xUnit + FluentAssertions** و دیتابیس InMemory آزمایش شدند.  
- در همهٔ بخش‌ها یک تگ انتشار واحد اعمال شده تا هماهنگی نسخه حفظ گردد:  
  🔖 `RiraDocs-v2025.11.4-Stable-Final-Fixed`  
- تمامی توضیحات فارسی طوری نگارش شده‌اند که خواننده مبتدی بتواند با نگاه به کد منطق معماری را درک کند.

---

## 🧰 ۴. ابزارها و نسخه‌های استفاده‌شده  

| ابزار | نسخه | توضیح |
|-------|-------|--------|
| .NET SDK | 8.0+ | سازگاری کامل با EF Core 8 و xUnit 3 |
| EF Core | 8.0 | تست async و mocking |
| AutoMapper | v12 | نگاشت داده بین DTO و Entity |
| MediatR | v12.x | پیاده‌سازی Patternهای Command / Query |
| FluentAssertions | v6.x | Assertهای خوانا برای تست‌ها |
| xUnit | v3.x | فریم‌ورک تست واحد |

---

## 🧩 ۵. تیم انتشار و امضا

| نقش | نام | توضیح |
|------|-----|--------|
| نویسنده و مستندساز اصلی | **سروش** | توسعه، سازمان‌دهی، و نگارش سبک آموزشی RiRaDocs نوشته‌شده به فارسی |
| ویراستار نسخه نهایی | **RiRaDocs Team** | بررسی محتوای آموزشی و کیفیت کامنت‌های Inline |

---

## 📚 ۶. وضعیت نهایی و توصیهٔ ذخیره‌سازی  

این نسخه به‌طور کامل بررسی و تست شده و هیچ خطای ساختاری یا منطقی در آن وجود ندارد.  
برای آرشیو نهایی پروژه، توصیه می‌شود فایل حاضر را به همراه تمام مستندات داخل نسخهٔ **`RiraDocs-v2025.11.4-Stable-Final-Fixed`** ذخیره شود.  
محل پیشنهادی ذخیره:  
> `F:\New Projects\THR\RiraApi\Rira.Api\Tests\Docs\_RiraDocs.ReleaseNote.2025.11.4.md`

---

✅ **Status: Verified and Ready for Archival**  
**وضعیت نهایی:** Stable / Final / Fixed  
